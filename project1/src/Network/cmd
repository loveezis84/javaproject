JAVA --version 틀릴 경우 컴파일 오류 발생
java 코드가 저장된 경로에 bin 안에 .class파일 만들어져 있으면 먼저 삭제 하시길 바랍니다.
javac 123.java : java를 실행 할 수 있도록  class로 변환 작업을 실행 합니다.


cmd -> 해당 경로 설정
cmd 명령어 관련 설명
1.통신 실행 port를 설정해야 합니다.(중복되지 않는값)
 netstat -an 으로 실행시 통신 port가 작동 되고 있는지 확인합니다.
2.만약 서버를 실행 하지 않은 상태에서 port가 작동되었을 경우 
 해당 포트를 정지시켜줘야 한다.
 netstat -a -o  ->pid번호 확인후에 
 taskkill /f /pid 번호값을 넣어줍니다.
 
3.실행은 명령어 java입력후 탭키를 눌러서 해당 폴더안에 있는 java파일을 실행시킵니다.